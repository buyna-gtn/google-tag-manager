import { Button } from "@/components/ui/button";

export const InfoButton: React.FC = () => {
  return (
    <Button id="user-info" className="font-semibold" variant="secondary">
      Get info!
    </Button>
  );
};
